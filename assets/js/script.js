// CODING QUIZ PROGRAM
//---- Declaring arrays with questions and answers (strings) so it is easier to add or replace questions in the future,
// maybe populating from a text file------
// ---------------------------------------------------------------------------------------
// Declare array with questions
var questionList = ["q1", "q2", "q3", "q4", "q5"];
// Declare array with answers
var answersList = ["a1", "a2", "a3", "a4", "a5"];
//-----------------------------------------------------------------------------------------

//-------------------------------------- DECLARATIONS --------------------------------------
// Declare object question
var question = {
  Q: "",
  A: "",
};

// Declare function populate (array questions, array answers) and returns array of question objects -> question Array
function populate(Qlist, AList, question) {}
// Declare function askQuestions (QA)
function askQuestions(questionArray) {}
// Declare function evaluateAnswer (QA)
function evaluateAnswer(questionArray) {}

// ----------------------- Execution ----------------------------

// Declare function startQuiz
function startQuiz() {}
// startQuiz()
